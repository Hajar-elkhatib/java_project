server:
  port: 8080
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true

spring:
  messages:
    encoding: UTF-8
  application:
   name: java_project
  data:
    mongodb:
      auto-index-creation: true
      uri: mongodb://127.0.0.1:27017/java_projet
  mvc:
    view:
      prefix: /WEB-INF/
      suffix: .jsp
ai:
  service:
    url: http://localhost:5000
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
